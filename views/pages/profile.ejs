<!DOCTYPE html>
<html lang="FR">
<head>

    <title>MayDay QPLT</title>
    <link rel="icon" type="image/png" href="images/favicon.png">
    <link rel="stylesheet" href="stylesheets/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

</head>
<body onload="birthdayParse()">

<div id="profile">

    <div class="side-bar">
        <div class="tabs">
            <h2></h2>
            <% if (locals.img) { %>
            <img src="data:image/jpeg;base64,<%=img %>" onclick="openProfile()" alt="">
            <% } %>
            <% if (!locals.img) { %>
            <img src="images/defaultProfilePic.png" onclick="openProfile()" alt="">
            <% } %>
            <div id="log-out" onclick="window.location.href='deconnexion';">DÉCONNEXION</div>
            <hr>
            <div onclick="openHome()">ACCUEIL</div>
            <div onclick="openWeather()">MÉTÉO</div>
            <div onclick="openTrends()">TENDANCES</div>
            <div onclick="openTransport()">TRANSPORTS</div>
            <div onclick="openCovid()">COVID-19</div>
        </div>
        <hr>
        <button id="mode-button" onclick="toggleMode()"></button>
    </div>

    <label>
        <input id="hideValueGetter3" type="hidden" name="birthday"
               value="<%=birthday %>"/>
    </label>

    <div id="profileDiv">
        <h2>Profil de <%=id %></h2>
        <button onclick="window.location.href='modifier_compte';">MODIFIER LE PROFIL</button>
        <div>
            <div>
                <span>Pseudo</span>
                <span><%=id %></span>
            </div>
            <div>
                <span>Nom</span>
                <span><%=lastName %></span>
            </div>
            <div>
                <span>Prénom</span>
                <span><%=firstName %></span>
            </div>
            <div>
                <span>Date de naissance</span>
                <span id="birthdaySpan"></span>
            </div>
            <div>
                <span>Pays</span>
                <span><%=country %></span>
            </div>
            <div>
                <span>Ville</span>
                <span><%=city %></span>
            </div>
        </div>
    </div>

</div>

<script src="https://kit.fontawesome.com/30f9fd1eba.js" crossorigin="anonymous"></script>
<script src="javascripts/app.js"></script>
</body>
</html>